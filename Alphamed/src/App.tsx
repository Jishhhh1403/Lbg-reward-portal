import AppRoutes from './routes/AppRoutes'
import './App.css'

/** Mobile-first healthcare portal — renders full-viewport like any responsive web app. */
export default function App() {
  return (
    <div className="app-root">
      <AppRoutes />
    </div>
  )
}
